---
import { createMarkdownProcessor } from "@astrojs/markdown-remark";
const processor = await createMarkdownProcessor();

export type Props = {
  /** We escape this, Nate should vet the content that goes in here anyways */
  content: string;
};

const html = await processor.render(Astro.props.content);
---

<Fragment set:html={html} />
